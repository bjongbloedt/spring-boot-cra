spring:
  application:
    name: api-widgets
  datasource:
    username: ${DB_USERNAME}
    password: mysecretpostgrespassword
    url: ${DB_URL}
  flyway:
    password: mysecretpostgrespassword
    user: ${DB_USERNAME}

debug: true